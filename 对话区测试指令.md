# 📋 对话区功能测试指令（给插件安装者）

> **用法**：复制下方「测试指令」整段，粘贴到 **DSH 新对话的输入框**发出即可。
> AI 会按指令输出全部测试元素（行内代码 / 代码块 / 长输出 / 任务栏），随后在 设置 → 主题 → 界面 → 对话区 逐项调色观察。
> 你自己发出去的这条消息本身就是「用户发言气泡」，可同时测「用户发言背景」。

---

## 📋 测试指令（复制以下全部内容）

请按以下 7 步输出内容，用于测试 dsh-theme-customizer 插件的对话区功能。每一步完成后继续下一步，不要省略。

1. 先输出一句话，里面包含至少 3 个**行内代码**（反引号包裹），例如：`npm install`、`console.log("hello")`、`--dsw-alias-markdown-inline-code`

2. 再输出一个**短代码块**（3~5 行）：
```javascript
// 代码块背景测试
function hello(name) {
  return "你好, " + name + "!"
}
console.log(hello("插件安装者"))
```

3. 输出一个**超长单行代码块**——代码块里只有一行，长度至少 600 个字符（例如一行里放一段很长的字符串），用于触发代码块内部的**横向滚动条**。

4. 输出一段**长文本**（15 行以上），内容随意（可以自我介绍或复述测试说明），用于观察对话区内容滚动。

5. 运行 **Pwsh 命令**输出 50 行内容，每行格式为 `测试行-N : AAAAAAAAA...（后面跟 80 个以上重复字母）`，用于触发：
   - terminal 代码块的**横向 + 纵向滚动条**
   - 对话区最外层的**横向滚动条**（内容超宽时出现）

6. 调用**任务清单工具**（todo）更新任务清单（列出 5 个测试任务），让对话区右侧**弹出任务栏**，用于测试「任务栏收起 / 任务栏展开」两态背景色。

7. 全部完成后，用一句话告诉我：以上内容已输出完毕，请到 设置 → 主题 → 界面 → 对话区 调整各选项颜色后观察效果。

---

## 🎯 对照测试清单

| 对话区选项 | 观察对象 |
|---|---|
| 用户发言背景 | 你自己发出的这条指令消息的气泡 |
| 行内代码背景 | 第 1 步的反引号片段 |
| 代码块背景 | 第 2 步短代码块 |
| 代码块滚动条 | 第 3 步超长行代码块（横滚）+ 第 5 步 terminal 块（横竖滚动） |
| 对话区滚动条 | 第 5 步超宽内容撑出的最外层横向滚动条 |
| 任务栏收起 / 展开 | 第 6 步弹出的任务栏（标题栏点击收起/展开） |
| 一键到底 | 滚动对话区到底部后右下角的 V 按钮 |
| 全部恢复官方默认 | 调色后一键还原 |

> ⚠️ 提示：若你的环境不允许 AI 运行 Pwsh / 任务清单工具，第 5、6 步会失败——此时可手动发一条超长消息（把第 3 步的超长行复制到普通文本）触发横向滚动，任务栏功能跳过即可。
